# 使用命令行参数作为配置数据源

## 可视化界面

项目-属性-调试-应用程序参数

## 支持的命令格式

* 无前缀的key=value模式
* --模式：--key=value 或 --key value
* 正斜杠模式：/key=value 或 /key value

注：等号分隔符和空格分隔符不能混用

## 命令替换模式

* 必须以单划线（-）或双划线（--）开头
* 映射字典不能包含重复key
